@model Client.ViewModels.LocationViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Create</h2>

@using (Html.BeginForm("Edit", "Location", FormMethod.Post))
{
    <table cellpadding="2" cellspacing="2">
        <thead>
            <tr>
                <th>
                    @Html.LabelFor(model => model.location.LocationName)
                </th>
                <td>
                    @Html.TextBoxFor(model => model.location.LocationName)
                </td>
            </tr>
            <tr>
                <td>
                    &nbsp;
                </td>
                <td>
                    <input type="submit" value="Save" />
                </td>
            </tr>
        </thead>
    </table>
}